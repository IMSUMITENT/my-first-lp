# CLAUDE.md

このファイルはこのプロジェクトで作業する際のルールを定義します。

## プロジェクト概要
- HTML / CSS / JavaScript で構成されたシンプルなプロジェクト

## コーディングルール
- CSS は `style.css` にまとめる（インラインスタイルや別ファイルへの分割はしない）
- JavaScript は `script.js` にまとめる（インラインスクリプトや別ファイルへの分割はしない）
- デザインはシンプルでモダンにする
- コードコメントは日本語で書く
- 動作確認用の `console.log` はコミット前に必ず削除する
